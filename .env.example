# Arcane Backend Configuration
ARCANE_HOST=localhost
ARCANE_PORT=8080
ARCANE_TOKEN=your_agent_token_here

# Optional: Override agent ID (if not set, will be auto-generated)
# AGENT_ID=my-custom-agent-id

# Optional: Enable debug logging
# DEBUG=true

# Optional: Use WebSocket (default: true)
# USE_WEBSOCKET=true

# Optional: TLS Configuration
# TLS_ENABLED=false

# Optional: Timing Configuration
# RECONNECT_DELAY=5s
# HEARTBEAT_RATE=30s

# Optional: Docker Compose projects path
# COMPOSE_BASE_PATH=data/agent/compose-projects